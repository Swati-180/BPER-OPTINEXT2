# Copy to client/.env and replace placeholders with real values.
# Do not commit client/.env to source control.

# Backend API URL — required for production builds (Vercel / Azure SWA)
# VITE_API_URL=https://your-backend.example.com

# Local development defaults to http://localhost:5000 when unset
